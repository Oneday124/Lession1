# Дана строка текста,напишите программу подсчета стоимости строки

sample_input = 'Тимур - лучший математик на свете!'
print(f'Стоимость строки: {round(len(sample_input) * 0.6 // 1)} руб. {round(len(sample_input) * 0.6 % 1 * 100)} коп.')
print(f'Стоимость строки: {(len(sample_input) * 60 )// 100} руб. {(len(sample_input) * 60) % 100} коп.')